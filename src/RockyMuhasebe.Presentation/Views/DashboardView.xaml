<UserControl x:Class="RockyMuhasebe.Presentation.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            
            <!-- KPI Kartları -->
            <UniformGrid Rows="1" Columns="4" Margin="0,0,0,10">
                
                <!-- Toplam Gelir -->
                <Border Style="{StaticResource CardPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                <Border.Background>
                                    <SolidColorBrush Color="#27AE60" Opacity="0.15"/>
                                </Border.Background>
                                <TextBlock Text="💰" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Toplam Gelir" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalRevenue}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Grid.Row="1" Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,12,0,8"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Border CornerRadius="4" Background="#E8F8F0" Padding="6,2">
                                <TextBlock Text="{Binding RevenueChange}" FontSize="11" Foreground="#27AE60" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text=" geçen aya göre" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Toplam Gider -->
                <Border Style="{StaticResource CardPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                <Border.Background>
                                    <SolidColorBrush Color="#E74C3C" Opacity="0.15"/>
                                </Border.Background>
                                <TextBlock Text="📉" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Toplam Gider" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalExpense}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Grid.Row="1" Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,12,0,8"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Border CornerRadius="4" Background="#FDEDEC" Padding="6,2">
                                <TextBlock Text="{Binding ExpenseChange}" FontSize="11" Foreground="#E74C3C" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text=" geçen aya göre" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Net Kâr -->
                <Border Style="{StaticResource CardPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                <Border.Background>
                                    <SolidColorBrush Color="#3498DB" Opacity="0.15"/>
                                </Border.Background>
                                <TextBlock Text="📊" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Net Kâr" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding NetProfit}" FontSize="22" FontWeight="Bold" Foreground="#27AE60"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Grid.Row="1" Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,12,0,8"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Border CornerRadius="4" Background="#E8F8F0" Padding="6,2">
                                <TextBlock Text="{Binding ProfitChange}" FontSize="11" Foreground="#27AE60" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text=" geçen aya göre" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Nakit Pozisyonu -->
                <Border Style="{StaticResource CardPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                <Border.Background>
                                    <SolidColorBrush Color="#F39C12" Opacity="0.15"/>
                                </Border.Background>
                                <TextBlock Text="🏦" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Nakit Pozisyonu" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding CashBalance}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Grid.Row="1" Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,12,0,8"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <TextBlock Text="Banka: " FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding BankBalance}" FontSize="11" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </UniformGrid>
            
            <!-- İkinci Satır Kartları -->
            <UniformGrid Rows="1" Columns="3" Margin="0,0,0,10">
                <!-- Müşteriler -->
                <Border Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👥" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Müşteri Bilgileri" Style="{StaticResource SubHeaderText}"/>
                        </StackPanel>
                        <Border Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Toplam Müşteri" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalCustomers}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Bu Ay Yeni" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding CustomerChange}" FontSize="28" FontWeight="Bold" Foreground="#27AE60"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Faturalar -->
                <Border Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🧾" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Fatura Durumu" Style="{StaticResource SubHeaderText}"/>
                        </StackPanel>
                        <Border Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Bekleyen" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding PendingInvoices}" FontSize="28" FontWeight="Bold" Foreground="#F39C12"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Vadesi Geçmiş" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding OverdueInvoices}" FontSize="28" FontWeight="Bold" Foreground="#E74C3C"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Stok -->
                <Border Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📦" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Stok Durumu" Style="{StaticResource SubHeaderText}"/>
                        </StackPanel>
                        <Border Height="1" Background="{StaticResource BorderLightBrush}" Margin="0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Toplam Ürün" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalProducts}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Düşük Stok" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding LowStockProducts}" FontSize="28" FontWeight="Bold" Foreground="#E74C3C"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </UniformGrid>
            
            <!-- Son İşlemler Tablosu -->
            <Border Style="{StaticResource CardPanel}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="🕐" FontSize="18" Margin="0,0,8,0"/>
                        <TextBlock Text="Son İşlemler" Style="{StaticResource SubHeaderText}"/>
                    </StackPanel>
                    
                    <DataGrid ItemsSource="{Binding RecentTransactions}" 
                              Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}"
                              CellStyle="{StaticResource ModernDataGridCell}"
                              MaxHeight="250">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tarih" Binding="{Binding Date}" Width="100"/>
                            <DataGridTextColumn Header="İşlem Türü" Binding="{Binding Type}" Width="120"/>
                            <DataGridTextColumn Header="Belge No" Binding="{Binding Document}" Width="170"/>
                            <DataGridTextColumn Header="Müşteri/Açıklama" Binding="{Binding Customer}" Width="*"/>
                            <DataGridTextColumn Header="Tutar" Binding="{Binding Amount}" Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
            
            <!-- Vadesi Geçmiş Faturalar -->
            <Border Style="{StaticResource CardPanel}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="⚠️" FontSize="18" Margin="0,0,8,0"/>
                        <TextBlock Text="Vadesi Geçmiş Faturalar" Style="{StaticResource SubHeaderText}" Foreground="#E74C3C"/>
                    </StackPanel>
                    
                    <DataGrid ItemsSource="{Binding OverdueInvoicesList}" 
                              Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}"
                              CellStyle="{StaticResource ModernDataGridCell}"
                              MaxHeight="200">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fatura No" Binding="{Binding InvoiceNo}" Width="170"/>
                            <DataGridTextColumn Header="Müşteri" Binding="{Binding Customer}" Width="*"/>
                            <DataGridTextColumn Header="Tutar" Binding="{Binding Amount}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Vade Tarihi" Binding="{Binding DueDate}" Width="120"/>
                            <DataGridTextColumn Header="Gecikme (Gün)" Binding="{Binding DaysOverdue}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
