<UserControl x:Class="RockyMuhasebe.Presentation.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <DockPanel Margin="20">
        <Border DockPanel.Dock="Top" Style="{StaticResource CardPanel}" Padding="15,10" Margin="8,0,8,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Yeni Ürün" Style="{StaticResource PrimaryButton}" Padding="16,8"/>
                <Button Content="✏️ Düzenle" Style="{StaticResource SecondaryButton}" Padding="16,8"/>
                <Button Content="🗑️ Sil" Style="{StaticResource DangerButton}" Padding="16,8"/>
                <Border Width="1" Background="{StaticResource BorderBrush}" Margin="10,4"/>
                <Button Content="📊 Stok Değeri" Style="{StaticResource SecondaryButton}" Padding="16,8"/>
                <Button Content="📤 Excel" Style="{StaticResource SecondaryButton}" Padding="16,8"/>
                <Button Content="🖨️ Yazdır" Style="{StaticResource SecondaryButton}" Padding="16,8"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardPanel}" Margin="8,0">
            <DataGrid ItemsSource="{Binding Products}" 
                      Style="{StaticResource ModernDataGrid}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                      RowStyle="{StaticResource ModernDataGridRow}"
                      CellStyle="{StaticResource ModernDataGridCell}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ürün Kodu" Binding="{Binding Code}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{StaticResource AccentBrush}"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Ürün Adı" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Kategori" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn Header="Stok" Binding="{Binding Stock}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Birim" Binding="{Binding Unit}" Width="70"/>
                    <DataGridTextColumn Header="Alış Fiyatı" Binding="{Binding BuyPrice}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Satış Fiyatı" Binding="{Binding SellPrice}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
